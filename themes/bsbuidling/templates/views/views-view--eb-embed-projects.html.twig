

  {% if rows %}
    <div class="view-content row" id = "org-projects">
      {% if header %}
        {{ header }}
      {% endif %}    
      {{ rows }}
    </div>
  {% elseif empty %}
    <div class="view-empty row" id = "org-projects">
      {{ empty }}
    </div>
  {% endif %}

  